- name: install and run nginx
  hosts: localhost
  tasks:
  - name: install nginx package
    ansible.builtin.package:
      name: nginax
      state: present
  - name: run nginx 
    ansible.builtin.service:
      name: nginx
      state: started